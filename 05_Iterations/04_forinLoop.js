const langs = 
{
    js : "Javascript",
    cpp : "C++",
    rb : "Ruby",
    go : "GoLang"
}

for (const key in langs) {
   console.log(langs[key]);
}

const arr = ["JS", "C++", "Java", "c#"];

for (const key in arr) {
    console.log(arr[key]);
}
